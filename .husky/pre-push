#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

set -e

echo "🔍 Running pre-push checks..."

echo "📝 Running mypy type checking..."
docker compose exec -T backend sh -lc "mypy ."

echo "🧪 Running pytest..."
docker compose exec -T backend sh -lc "pytest tests/ -v"

echo "✅ Pre-push checks completed!"
