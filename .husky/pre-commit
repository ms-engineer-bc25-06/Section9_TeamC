#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

set -e

echo "🎨 FE: lint-staged"
npx lint-staged --cwd frontend --config frontend/.lintstagedrc.cjs

echo "🐍 BE: ruff"
docker compose exec -T backend sh -lc "ruff check --fix . && ruff format ."

echo "✅ Pre-commit checks completed!"
